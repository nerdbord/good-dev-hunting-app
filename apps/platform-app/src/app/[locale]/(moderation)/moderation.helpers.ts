import { type ModerationProfilesStore } from '@/app/[locale]/(moderation)/_stores/moderation-profiles.store'

export const getModerationCurrentState = (
  state: ModerationProfilesStore,
): ModerationProfilesStore => state
